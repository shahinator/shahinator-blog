<h1 align="center">Welcome to <strong><%= @user.username %></strong> 's Profile Page </h1>
<hr>
<div class="row">
   <div class="col-md-4 col-md-offset-4 text-center">
        <%= gravatar_for @user, size: 150 %>
   </div> 
</div>
<h4 align="center"> <strong><%= @user.username %></strong> 's Articles </h4>
<%=  render 'articles/article', obj: @user.articles %>
